# Prompt the user to input a string
write = input("Write...:")

# Initialize a counter to store the number of characters in the input
count = 0

# Iterate through each character in the input string
for char in write:
    # Increment the counter by 1 for each character
    count += 1

# Print the total number of characters in the input
print(count)
